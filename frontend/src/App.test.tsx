import firebase from 'firebase';
import React from 'react';
import ReactDOM from 'react-dom'
import App from './App';

// TODO figure out how to initialize firebase for unittests
// Seems to fail unless I let it actually connect to google's services in test, which I do not want to do

test('renders the home page', () => {
    //const div = document.createElement('div');
    //ReactDOM.render(<App/>, div);
});
